<leads>{% for lead in leads %}
 <lead>
  <ts>{{ lead.ts }}</ts>
  <name>{{ lead.name }}</name>
  <contactperson>{{ lead.contactperson }}</contactperson>
  <contactnumber>{{ lead.contactnumber }}</contactnumber>
  <area>{{ lead.area }}</area>
  <email>{{ lead.email }}</email>
  <status>{{ lead.status }}</status>
  <user>{{ lead.user }}</user>
  <lat>{{ lead.lat }}</lat>
  <lng>{{ lead.lng }}</lng>
 </lead>{% endfor %}
</leads>